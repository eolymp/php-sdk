<?php
# Generated by the protocol buffer compiler. DO NOT EDIT!
# See https://github.com/eolymp/contracts/tree/main/cmd/protoc-gen-php-esdk for more details.

namespace Eolymp\Course;

    /**
     * RunService executes a student's code against input they choose, without submitting it.
     *
     * A run is not a submission: it is never graded and never counted towards the student's progress on the
     * course. It exists so a student can try their program on an example test or on input they typed themselves
     * before deciding to submit. Runs are asynchronous — CreateRun hands back an id, DescribeRun polls it and
     * WatchRun streams it until it settles.
     *
     * A run is addressed by the material rather than by the problem, because the material is what references the
     * problem and what decides whether the student may reach it at all. The same operation exists for a problem
     * on its own (eolymp.atlas.RunService) and for one inside a contest (eolymp.judge.RunService).
     */
class RunServiceClient {

    /** @var string base URL */
    private $url;

    /** @var callable RPC client */
    private $invoker;

    /**
     * @param string   $url     defines base URL for service
     * @param callable $invoker provides transport implementation for calls
     */
    public function __construct($url, $invoker)
    {
        $this->url = $url;
        $this->invoker = $invoker;
    }

    /**
     * CreateRun compiles the source and starts executing it, returning the id to follow it by. Input is given
     * either literally, as a reference to uploaded data, or as an example test of the problem; for an
     * interactive problem an example is the only accepted input, and the problem's interactor is attached.
     *
     * @param CreateRunInput $input message
     * @param array $context request parameters
     *
     * @return CreateRunOutput output message
     */
    public function CreateRun(CreateRunInput $input, array $context = [])
    {
        $path = "/courses/".rawurlencode($input->getCourseId())."/materials/".rawurlencode($input->getMaterialId())."/runs";

        // Cleanup URL parameters to avoid any ambiguity
        $input->setCourseId("");
        $input->setMaterialId("");

        $context['name'] = "eolymp.course.RunService/CreateRun";
        $context['path'] = $path;

        return call_user_func($this->invoker, "POST", $this->url.$path, $input, CreateRunOutput::class, $context);
    }

    /**
     * DescribeRun returns the run as it stands, which is how a caller polls one to completion.
     *
     * @param DescribeRunInput $input message
     * @param array $context request parameters
     *
     * @return DescribeRunOutput output message
     */
    public function DescribeRun(DescribeRunInput $input, array $context = [])
    {
        $path = "/courses/".rawurlencode($input->getCourseId())."/materials/".rawurlencode($input->getMaterialId())."/runs/".rawurlencode($input->getRunId());

        // Cleanup URL parameters to avoid any ambiguity
        $input->setCourseId("");
        $input->setMaterialId("");
        $input->setRunId("");

        $context['name'] = "eolymp.course.RunService/DescribeRun";
        $context['path'] = $path;

        return call_user_func($this->invoker, "GET", $this->url.$path, $input, DescribeRunOutput::class, $context);
    }

}
