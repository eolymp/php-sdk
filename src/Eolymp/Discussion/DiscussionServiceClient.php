<?php
# Generated by the protocol buffer compiler. DO NOT EDIT!
# See https://github.com/eolymp/contracts/tree/main/cmd/protoc-gen-php-esdk for more details.

namespace Eolymp\Discussion;

    /**
     * internal
     *
     * DiscussionService holds the comment thread of anything that can be discussed, and knows nothing about
     * what that thing is. The service owning the discussable — eolymp.atlas.DiscussionService for a problem,
     * eolymp.content.DiscussionService for a post — decides whether the caller may see or write to it, then
     * names the thread here. That is why this service has no http rules: it is reached over gRPC only.
     *
     * A thread is named by the canonical url of what is being discussed, so threads never collide across
     * spaces and a discussable that has never been commented on simply has an empty thread rather than
     * needing to be created first.
     */
class DiscussionServiceClient {

    /** @var string base URL */
    private $url;

    /** @var callable RPC client */
    private $invoker;

    /**
     * @param string   $url     defines base URL for service
     * @param callable $invoker provides transport implementation for calls
     */
    public function __construct($url, $invoker)
    {
        $this->url = $url;
        $this->invoker = $invoker;
    }

}
